services:
  # Backend API
  - type: web
    name: aiquiniela-api
    runtime: node
    buildCommand: npm install --include=dev && cd packages/backend && npx prisma generate && npm run build
    startCommand: cd packages/backend && npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: NODE_ENV
        value: production
    healthCheckPath: /health

  # Frontend SPA
  - type: web
    name: aiquiniela-frontend
    runtime: static
    buildCommand: npm install && cd packages/frontend && npm run build
    staticPublishPath: packages/frontend/dist
    envVars:
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
